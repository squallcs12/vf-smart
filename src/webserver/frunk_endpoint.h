#ifndef WEBSERVER_FRUNK_ENDPOINT_H
#define WEBSERVER_FRUNK_ENDPOINT_H

#include <ESPAsyncWebServer.h>

// Register front trunk (frunk) unlock endpoint
void registerFrunkEndpoint(AsyncWebServer& server);

#endif // WEBSERVER_FRUNK_ENDPOINT_H
