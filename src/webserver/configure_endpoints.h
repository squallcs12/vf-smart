#ifndef WEBSERVER_CONFIGURE_ENDPOINTS_H
#define WEBSERVER_CONFIGURE_ENDPOINTS_H

#include <ESPAsyncWebServer.h>

// Register configuration and restart endpoints
void registerConfigureEndpoints(AsyncWebServer& server);

#endif // WEBSERVER_CONFIGURE_ENDPOINTS_H
