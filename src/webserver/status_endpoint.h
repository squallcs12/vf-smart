#ifndef WEBSERVER_STATUS_ENDPOINT_H
#define WEBSERVER_STATUS_ENDPOINT_H

#include <ESPAsyncWebServer.h>

// Register status endpoint
void registerStatusEndpoint(AsyncWebServer& server);

#endif // WEBSERVER_STATUS_ENDPOINT_H
