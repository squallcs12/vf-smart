#ifndef WEBSERVER_WINDOW_ENDPOINTS_H
#define WEBSERVER_WINDOW_ENDPOINTS_H

#include <ESPAsyncWebServer.h>

// Register window control endpoints
void registerWindowEndpoints(AsyncWebServer& server);

#endif // WEBSERVER_WINDOW_ENDPOINTS_H
