#ifndef WEBSERVER_LIGHT_REMINDER_ENDPOINT_H
#define WEBSERVER_LIGHT_REMINDER_ENDPOINT_H

#include <ESPAsyncWebServer.h>

// Register light reminder control endpoint
void registerLightReminderEndpoint(AsyncWebServer& server);

#endif // WEBSERVER_LIGHT_REMINDER_ENDPOINT_H
